# `ops/prometheus/`

Prometheus scrape configuration for local + CI metrics tests. Populated in Phase 7.

Will contain:
- `scrape.yml` — scrape targets for `irc-server` (`127.0.0.1:9772`) and `irc-bnc` (`127.0.0.1:9773`)
